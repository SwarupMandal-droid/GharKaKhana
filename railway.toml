[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120'"

healthcheckPath = "/"

healthcheckTimeout = 100

restartPolicyType = "on_failure"

restartPolicyMaxRetries = 3